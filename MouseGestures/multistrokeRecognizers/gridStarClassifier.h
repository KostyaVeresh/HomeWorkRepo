#pragma once

#include "abstractRecognizer.h"
#include "GeometricForms.h"

//template <class GesManager>
class GridStarClassifier
{
public:
	GridStarClassifier();
	//GridStarClassifier(const PathVector &path);
	//double getDistance(const GridStarClassifier &classifier);


private:
	GridStarClassifier(QList<int> key);

};
